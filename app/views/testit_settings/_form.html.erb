<%= error_messages_for 'setting' %>
<div class="box tabular">
    <p><%=f.select :bug_tracker, @project.trackers.collect {|t| [t.name, t.id]}, :required => true, :selected => @testit_settings.bug_tracker %></p>
  <p><%=f.select :requirement_tracker, @project.trackers.collect {|t| [t.name, t.id.to_s]}, {}, :multiple => true, :size => 5, :selected => @testit_settings.requirement_tracker %></p>
  <p><%=f.select :test_suite_tracker, @project.trackers.collect {|t| [t.name, t.id.to_s]}, :required => true, :selected => @testit_settings.test_suite_tracker %></p>
  <p><%=f.select :test_case_tracker, @project.trackers.collect {|t| [t.name, t.id.to_s]},  :required => true, :selected => @testit_settings.test_case_tracker %></p>
  <p><%=f.select :test_plan_tracker, @project.trackers.collect {|t| [t.name, t.id.to_s]},  :required => true, :selected => @testit_settings.test_plan_tracker %></p>
  <p><%=f.select :test_run_tracker, @project.trackers.collect {|t| [t.name, t.id.to_s]},   :required => true, :selected => @testit_settings.test_run_tracker %></p>
</div>
  
<%= submit_tag l(:button_save) %>
